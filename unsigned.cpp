#include <iostream.sh>
#include <conio.h>
{
        unsigned int jumlah, harga;
        int total;
        jumlah = 10;
        harga = 5000; // jumlah >= 0
        total = jumlah * harga; // harga >=0
        cout << "Total = " << total;
        getch();
        return 0;
}
